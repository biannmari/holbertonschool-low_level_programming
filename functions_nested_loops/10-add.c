#include "main.h"

/**
* add - adds two integers and returns the result
* @a: first integer, to be addes to b
* @b: second integerto be added to a
* Return: value of 'add'sum of a and b
*/

int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}
